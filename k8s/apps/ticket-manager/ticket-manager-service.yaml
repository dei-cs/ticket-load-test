apiVersion: v1
kind: Service
metadata:
  name: ticket-manager
  namespace: ticket-system
spec:
  selector:
    app: ticket-manager
  ports:
    - port: 8001
      targetPort: 8001
      nodePort: 30001
  type: NodePort

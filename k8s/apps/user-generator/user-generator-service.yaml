apiVersion: v1
kind: Service
metadata:
  name: user-generator
  namespace: ticket-system
spec:
  selector:
    app: user-generator
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30000
  type: NodePort

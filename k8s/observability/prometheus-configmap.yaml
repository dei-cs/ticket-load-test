apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: ticket-system
data:
  prometheus.yaml: |
    global:
      scrape_interval: 5s

    scrape_configs:
      - job_name: "otel-collector"
        static_configs:
          - targets: ["otel-collector:9464"]
